import { SignUpForm } from './signup-form';

/**
 * Sign-up view (Req 1.1–1.4).
 */
export default function SignUpPage() {
  return (
    <main className="container flex min-h-screen items-center justify-center py-12">
      <SignUpForm />
    </main>
  );
}
